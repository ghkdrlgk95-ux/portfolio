# 황기하 — SI 디지털 트윈 3D 포트폴리오

**공개 주소 : <https://ghkdrlgk95-ux.github.io/portfolio/>**

이 저장소는 GitHub Pages 로 서비스되는 포트폴리오 사이트입니다.
사이트는 손으로 고치지 않고, **전용 편집기에서 수정하면 자동으로 이 저장소에 올라갑니다.**

| 파일 | 역할 |
|---|---|
| `index.html` | 공개되는 사이트 — **자동 생성되므로 직접 수정하지 마세요** |
| `images/` | 사이트용 이미지 (편집기가 자동 추출·축소) |
| `videos/` | 영상 파일 (GitHub 제한: 파일당 100MB) |
| `editor.html` | 편집기 사본 (백업용) |
| `README.md` | 이 문서 |

편집기 본체(`HwangGiHa_Portfolio.html`, `편집기 실행.bat`, `_editor_server.py`)는
용량 때문에 저장소에 넣지 않고 로컬 폴더에 둡니다.

```
D:\YMX_gh 개인\GH자료\00_개인자료\YMX_황기하_자료\YMX 황기하 포폴\
```

---

## 아래는 편집기 사용법입니다

## 1. 여는 방법

### ⚠️ `편집기 실행.bat` 을 더블클릭하세요

| 여는 방법 | 주소 | 저장 |
|---|---|---|
| **`편집기 실행.bat`** | `http://127.0.0.1:8765/…` | ✅ 저장 → 커밋 → 사이트 반영 |
| `HwangGiHa_Portfolio.html` 직접 | `file:///D:/…` | ❌ **안 됨** |

HTML 파일을 직접 열면 저장 버튼을 눌러도 이렇게만 뜹니다.

> `이 주소로는 파일에 못 씁니다 — '편집기 실행.bat'으로 여세요`

브라우저는 보안상 자기가 연 파일을 덮어쓸 수 없습니다.
그래서 `.bat` 이 작은 서버를 띄우고, **그 서버가 대신 파일에 쓰고 git 에 올립니다.**

실행하면 검은 창이 뜨고 브라우저가 자동으로 열립니다.
**편집하는 동안 검은 창은 켜둬야 합니다.** 닫으면 저장이 안 됩니다.

---

## 2. 전체 흐름

```
편집기 실행.bat  더블클릭
      ↓
브라우저에서 수정
      ↓
[저장] 클릭
      ├─ HwangGiHa_Portfolio.html      (편집용 원본)
      ├─ git 저장소의 index.html         (공개 사이트용)
      ├─ git 저장소의 images/            (이미지 자동 추출·축소)
      ├─ git commit                     (자동)
      └─ git push                       (자동)
      ↓
1~2분 뒤  https://ghkdrlgk95-ux.github.io/portfolio/  반영
```

저장 버튼을 누르면 상태가 순서대로 표시됩니다.

```
저장 중… 23.8MB
저장 + 커밋 완료, 올리는 중 (커밋 1개)
저장 + 사이트에 올림 (커밋 1개) - 1~2분 뒤 반영
```

마지막 줄까지 나오면 끝입니다. 따로 하실 일은 없습니다.

---

## 3. 편집 방법

### 글자

아무 글자나 **클릭하면 바로 수정**됩니다. 제목, 설명, 지표 값, 메타바 항목명, 연락처의 Email·Tel 라벨까지 — 글자가 있는 곳은 전부요.

#### 글자 크기 툴바

글자를 클릭하면 바로 위에 작은 검은 툴바가 뜹니다. (위에 자리가 없으면 아래에 뜹니다)

```
A−  14px  A+  ↺  ×
```

| 버튼 | 하는 일 |
|---|---|
| **A−** / **A+** | 글자 **1px** 씩 작게 / 크게 (10 ~ 72px) |
| 가운데 숫자 | 지금 크기 |
| **↺** | 원래 크기로 |
| **×** | 소제목 삭제 (소제목에서만 보임) |

바꾼 크기는 저장하면 사이트에도 그대로 적용됩니다.

#### 줄바꿈

- **Enter** 는 줄바꿈입니다. 제목·문단·글 상자 어디서든 줄이 나뉘고, 저장해도 그대로 남습니다.
- 글 **앞**에 넣은 빈 줄은 그대로 저장됩니다.
- 글 **끝**의 빈 줄은 저장할 때 정리됩니다. (브라우저가 커서 자리용 가짜 줄바꿈을 하나 더 넣기 때문)

#### 글 상자

- 빈 글 상자는 **상자 아무 데나 클릭**하면 입력이 시작됩니다.
- 상자에 마우스를 올리면 **상자 바깥 왼쪽**에 ▲ ▼ × 가 세로로 뜹니다. 글자와 겹치지 않습니다.
- 크기를 조절해 둔 상자도 똑같이 동작합니다. (지표 카드의 ▲▼는 오른쪽 위)

### 소제목

`와이어프레임 · 최적화 맵 · Unity 씬` 처럼 이미지·영상 위에 붙는 작은 회색 제목입니다.

| 동작 | 방법 |
|---|---|
| 추가 | 섹션 맨 아래 **`+ 소제목 추가`** → 바로 입력 |
| 크기 | 클릭 → 툴바 **A− / A+** |
| 옮기기 | 소제목은 **바로 아래 칸에 붙어** 다닙니다. 그 칸의 ▲▼ 를 누르면 같이 이동 |
| 삭제 | 클릭 → 툴바 **×** |

### 이미지 칸

| 동작 | 방법 |
|---|---|
| 이미지 넣기 / 바꾸기 | 칸을 **클릭** → 파일 선택 |
| 위치 옮기기 | 이미지를 **끌기** |
| 확대 / 축소 | 이미지 위에서 **마우스 휠** |
| 가로만 조절 | **오른쪽 모서리** 끌기 (↔) |
| 세로만 조절 | **아래쪽 모서리** 끌기 (↕) |
| 가로·세로 같이 | **우하단 꼭짓점** 끌기 (⤡) |
| 원래 크기로 | 아무 모서리나 **더블클릭** |
| 잠그기 | 🔓 클릭 → 🔒 |
| 삭제 | × 클릭 |

**🔒 잠그면** 이동·확대·크기조절·삭제·교체가 전부 막힙니다.
위치를 잡아둔 이미지가 실수로 틀어지는 걸 막으려면 잠가두세요.

### 동영상 칸

- **재생**은 편집 중에도 됩니다. 그냥 누르면 재생됩니다.
- **링크 바꾸기**는 우상단 **🔗** 버튼으로 합니다.
- 넣을 수 있는 주소
  - `videos/파일이름.mp4` — 저장소에 있는 영상 (권장)
  - YouTube · Vimeo 주소
  - 구글 드라이브 **파일** 공유 링크 (폴더 링크는 안 됨)

### 항목 추가

각 섹션 맨 아래에 버튼이 있습니다.

- `+ 글 상자 추가`
- `+ 소제목 추가` — `와이어프레임 · 최적화 맵` 같은 작은 회색 제목
- `+ 큰 이미지 추가` — 가로 전체를 쓰는 이미지 칸
- `+ 동영상 추가`
- `+ 이미지 추가` — 이미지 묶음 안에 한 칸 더
- `+ 지표 추가` — 숫자 카드 한 장 더

새 항목은 **버튼 앞에** 들어가고, 버튼은 항상 맨 뒤에 남습니다.

### 섹션 순서 바꾸기

섹션 맨 위 손잡이를 씁니다.

```
⠿⠿ 섹션 이동 5 / 10   ▲ ▼
```

- **▲** 한 칸 위로 / **▼** 한 칸 아래로
- 지금 몇 번째인지 항상 숫자로 표시됩니다
- 옮기면 `섹션 이동됨 · 5번째 → 4번째` 로 알려줍니다
- 맨 위·맨 아래에서는 해당 버튼이 흐리게 비활성됩니다

가까운 거리는 손잡이를 잡고 끌어도 됩니다. 다만 섹션이 길어서 **버튼이 확실합니다.**

### 프로젝트 바로가기

첫 화면 이미지 밑에 있는 썸네일 카드 6장입니다. 클릭하면 해당 프로젝트로 내려갑니다.

| 할 수 있는 것 | 방법 |
|---|---|
| 카드 제목 · 한 줄 설명 고치기 | 편집 모드에서 글자 클릭 (편집 중에는 눌러도 이동하지 않습니다) |
| 카드 순서 · 썸네일 이미지 바꾸기 | 편집기로는 안 됩니다 — 요청해 주세요 |
| 새 프로젝트 섹션 추가 | 섹션은 추가되지만 바로가기 카드는 자동으로 안 생깁니다 — 요청해 주세요 |

섹션 순서를 ▲▼ 로 바꿔도 바로가기는 그대로 동작합니다 (이름으로 찾아가기 때문).

---

## 4. 문제가 생겼을 때

### 저장이 안 됩니다

**검은 창이 닫혔는지 확인하세요.** 닫혔으면 `편집기 실행.bat` 을 다시 실행하고 브라우저에서 **F5**.

### `이 탭은 오래된 내용입니다 (탭 N / 파일 M)`

탭을 오래 열어둔 사이 파일이 바뀌었다는 뜻입니다. **당황하지 않으셔도 됩니다.**

- 파일은 덮어쓰이지 **않았습니다**
- 그 탭의 수정 내용은 `_backup` 폴더에 `충돌본_날짜_탭N_파일M.html` 로 **자동 보관**됩니다
- **F5** 로 새로고침한 뒤 이어서 작업하시면 됩니다

### `GitHub 로그인이 저장돼 있지 않습니다`

터미널에서 한 번만 실행해 로그인해 두세요. 이후로는 자동입니다.

```
git -C "D:/YMX_gh 개인/GH자료/git/portfolio" push origin main
```

로그인이 안 돼 있어도 **저장과 커밋은 정상 동작**하니 작업물은 안전합니다.
나중에 위 명령 한 번이면 밀린 게 전부 올라갑니다.

### 사이트에 바로 안 보입니다

- GitHub Pages 재빌드에 **1~2분** 걸립니다
- 브라우저 캐시가 **최대 10분** 남습니다

바로 확인하려면 **시크릿 창**으로 열거나 **Ctrl + F5**.

---

## 5. 폴더 구성

### 편집 폴더 — `YMX 황기하 포폴`

| 파일 | 역할 |
|---|---|
| `HwangGiHa_Portfolio.html` | **편집용 원본** (이미지 내장, 약 25MB) |
| `편집기 실행.bat` | 이걸 더블클릭해서 실행 |
| `_editor_server.py` | 저장·커밋·push 를 처리하는 스크립트 |
| `_backup/` | 저장할 때마다 직전 파일 보관 (최근 5개) |
| `사용법.md` | 이 문서 |

### 저장소 — `GH자료\git\portfolio`

| 파일 | 역할 |
|---|---|
| `index.html` | **공개되는 사이트** (저장할 때 자동 생성) |
| `images/` | 사이트용 이미지 (자동 추출·축소) |
| `videos/` | 영상 파일 |
| `editor.html` | 편집기 사본 (백업용) |

`index.html` 과 `images/` 는 **직접 건드리지 마세요.** 저장할 때마다 자동으로 다시 만들어집니다.

---

## 6. 알아두면 좋은 것

### 영상 넣기

영상은 HTML 에 넣을 수 없어서 **파일로 저장소에 둡니다.**

- 위치 : `GH자료\git\portfolio\videos\`
- **GitHub 제한 : 파일당 100MB** — 넘으면 안 올라갑니다
- 원본이 크면 압축이 필요합니다 (지금 있는 것들은 3.7GB → 255MB 로 줄여 넣은 것)
- 편집기에는 `videos/파일이름.mp4` 로 넣으면 됩니다
- 같은 이름의 `.jpg` 를 옆에 두면 재생 전 첫 화면으로 쓰입니다

### 화면 폭 대응

어느 화면에서든 자동으로 맞춰집니다. **따로 하실 건 없습니다.**

| 화면 | 동작 |
|---|---|
| 큰 모니터 (1120px 이상) | 편집기에서 잡은 그대로 (본문 폭 980px) |
| 작은 노트북 (681~1120px) | 칸 폭이 화면에 맞춰 줄어듦. 2열은 2열 그대로 |
| 폰 (680px 이하) | 칸들이 세로로 하나씩 쌓임. 바로가기는 2열 |

- 이미지는 가로세로 비율이 유지됩니다
- 글자가 길면 칸이 늘어납니다 (잘리지 않음)
- PC 에서 잡아둔 이미지 확대·위치는 **폰에서만** 달라집니다. 좁은 화면에 그대로 쓸 수 없어서, 폰에서는 이미지가 칸을 채우도록 처리됩니다

### 글자 크기 · 이미지 폭 기준

새 항목을 만들 때 이 기준에 맞추면 다른 섹션과 톤이 맞습니다. (툴바 A− / A+ 로 조절)

| 요소 | 크기 |
|---|---|
| 프로젝트 제목 | 30px |
| 설명 줄 · 글 상자 본문 · 메타바 **값** (모델링 · 최적화 …) | 17px |
| 소제목 · 파이프라인 줄 · 경력 칼럼 제목 | 15px |
| 배지 (Unity 등) · 지표 라벨 | 14px |
| 메타바 **항목명** (본인 역할 · 사용 툴 …) | 13px |

항목명(13)과 값(17)이 두 단계 차이 나야 훑어 읽힙니다. 같은 크기로 맞추면 위계가 사라집니다.

이미지 칸은 **가로 전체 980px** 아니면 **2열 484px** 둘 중 하나로 두면 줄이 맞습니다.

### 백업

- 저장할 때마다 직전 파일이 `_backup` 에 남습니다 (최근 5개)
- 편집본이 개당 25MB 라 폴더가 100MB 를 넘길 수 있습니다
- **지워도 됩니다.** 편집·저장에 영향 없고 다음 저장 때 새로 만들어집니다

### 링크 공유

주소를 걸어둔 곳은 저장만 하면 **전부 자동으로 최신**이 됩니다.

| 어떻게 공유했나 | 자동 갱신 |
|---|---|
| 주소를 적어둠 (이력서, 채용사이트 링크란, 메일 서명) | ✅ |
| HTML 파일을 첨부해서 보냄 | ❌ |
| PDF 로 내보내 올림 | ❌ |
| 채용사이트에 파일 업로드 | ❌ |

**파일이 아니라 주소로 공유**해 두시는 게 앞으로 편합니다.

---

## 7. 툴바

```
포트폴리오 편집기    편집  보기  저장  내보내기  PDF
```

| 버튼 | 하는 일 |
|---|---|
| **편집** | 편집 모드 (손잡이·버튼이 보임) |
| **보기** | 방문자에게 보이는 화면으로 미리보기 |
| **저장** | 파일 기록 + 커밋 + 사이트 반영 — **평소엔 이것만 쓰면 됩니다** |
| **내보내기** | 편집 도구가 빠진 HTML 파일 한 개로 내려받기 |
| **PDF** | 인쇄 창으로 PDF 저장 |

---

## 8. 변경 이력

### 2026-09-17

**편집기 — 새로 생긴 것**

- 글자 크기 툴바 (A− / A+ / ↺ / ×) — 글자가 있는 곳 전부, 1px 단위
- `+ 소제목 추가` 버튼
- 빈 글 상자 아무 데나 클릭하면 입력 시작
- Enter = 줄바꿈 (제목·문단·글 상자 공통). 글 앞의 빈 줄은 그대로 저장, 끝의 빈 줄만 정리

**편집기 — 고쳐진 것**

- 새 글 상자를 클릭하면 이동 버튼이 대신 눌리던 문제 → ▲▼× 를 상자 바깥 왼쪽으로
- 크기 조절한 글 상자에서 버튼·손잡이가 상자 폭 전체의 어두운 띠로 늘어나 클릭을 가로채던 문제
- 글 상자 안 줄바꿈이 저장하면 사라지던 문제
- 소개 문단에서 Enter 로 나눈 줄이 저장 후 편집 불가가 되던 문제 (+ 재발 방지)
- 연락처 Email · Tel 라벨 편집 가능
- Whale 브라우저가 끼워 넣는 태그가 저장본에 섞이던 것 차단

**포트폴리오 구성 (UI 리뷰 1~8번 반영)**

- 첫 화면 이미지 → 기계설비 Unity 씬. 있던 주택 렌더는 건축 인테리어 섹션 맨 앞으로
- 첫 화면에 **프로젝트 바로가기** 6장
- 섹션 순서: 기계설비 AR → **LG 프로젝트**(새 섹션, 링크 글 상자 이동) → 한기대 경도시험 → MXSpace → 건축 인테리어 → 뉴딜 VR → 경력
- 경도시험 영상 4개 → 2열 격자, 포스터는 실제 장면 프레임으로 교체
- 이미지 칸 크기 통일 — 2열 쌍 484px·같은 높이, 가로 전체 980px
- 경도시험 · MXSpace · 건축 인테리어 · 뉴딜 VR 에 설명 글 상자 추가 (페이지에 있던 사실만으로 작성 — 직접 보강 권장)
- 글자 위계 — 제목 30 · 설명/본문/메타바 값 17 · 소제목 15 · 배지/지표 라벨 14 · 메타바 항목명 13
- 지표 라벨 — `YMX_누적 프로젝트` → `YMX 누적 프로젝트`, `Digital twin` → `디지털 트윈`
- 작은 노트북(681~1120px)에서 가로로 넘치던 것 수정

### 2026-09-18

**영상**

- 편집기 안에서 영상 슬라이드바를 움직여도 그 위치로 이동하지 않던 문제 수정 — 편집기 서버(`_editor_server.py`)가 구간 요청(Range)에 응답하도록 변경. 공개 사이트는 원래 정상이었음
- 경도시험 영상 순서를 영상 내용 기준으로 정리 — **브리넬 → 로크웰 → 비커스 → 쇼어** (브리넬 영상만 조작법 안내를 처음부터 담고 있어 첫 모듈로 확인)
- 경도시험 첫 화면(포스터) 4장을 각 시험기가 **눈높이 정면**으로 보이는 장면으로 교체 — 브리넬 · 로크웰 · 비커스(3분 1초) · 쇼어(41초)
- 첫 화면 이미지는 `videos/이름.jpg` 파일이라 편집기로는 못 바꿉니다. 바꾸고 싶은 시각(예: 3분 1초)만 말씀해 주시면 됩니다
